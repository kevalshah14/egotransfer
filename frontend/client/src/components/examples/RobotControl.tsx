import RobotControl from '../RobotControl';

export default function RobotControlExample() {
  // No mock data - real data should be passed from parent component
  return <RobotControl annotations={[]} />;
}